clases: 
1. parser que deje los archivos como lista o string
2. ver si es necesario un dirparser
2. un filedict (hereda de MutableMapping) para guardar los contenidos de un archivo
2.a. el filedict deberia llamar al parser en el setitem
3. un dirdict que guarde los contenidos del directorio, donde archivo(key): contenido (value)
3. el dirdict deberia heredar del filedict

la clase de fieldict:
1. que el setitem sea abrir archivo (key) y guardar los contenidos como valor (value)
2. el getitem sera simplemente agarrar los componentes de esa key
