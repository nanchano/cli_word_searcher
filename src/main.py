from parser.parser import Parser
from filesystem_dicts.file_dicts import *

def main() -> None:
    file_dict = FileDict('inputs/')
    dir_dict = DirDict('inputs/')
    
    return

if __name__ == '__main__':
    main()
